<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>GameZone Arena</title>

<!-- Bootstrap 5 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<style>
body { transition: 0.3s; }

/* ===== HERO FULLSCREEN SLIDER ===== */
.hero-section{
    position: relative;
    height: 100vh;
    overflow: hidden;
}

.hero-img{
    height: 100vh;
    object-fit: cover;
}

.hero-content{
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
}

.carousel::after{
    content: "";
    position: absolute;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.55);
    top: 0;
    left: 0;
}

/* Card Hover */
.card img{ transition:0.4s; }
.card:hover img{ transform:scale(1.05); }

/* Profile */
.profile-header{
    height:250px;
    background:url('https://images.unsplash.com/photo-1493711662062-fa541adb3fc8')
                center/cover no-repeat;
}
.profile-header img{
    border:4px solid white;
}
</style>
</head>
<body>

<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<div class="container">
<a class="navbar-brand" href="#">GameZone</a>
<ul class="navbar-nav ms-auto">
<li class="nav-item"><a class="nav-link active" href="#home">Home</a></li>
<li class="nav-item"><a class="nav-link" href="#games">Games</a></li>
<li class="nav-item"><a class="nav-link" href="#profile">Profile</a></li>
</ul>
</div>
</nav>

<!-- ================= FULLSCREEN HERO ================= -->
<section id="home" class="hero-section">

<div id="heroCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel" data-bs-interval="3000">

<div class="carousel-inner">

<div class="carousel-item active">
<img src="https://images.unsplash.com/photo-1542751371-adc38448a05e" class="d-block w-100 hero-img">
</div>

<div class="carousel-item">
<img src="https://images.unsplash.com/photo-1511512578047-dfb367046420" class="d-block w-100 hero-img">
</div>

<div class="carousel-item">
<img src="https://images.unsplash.com/photo-1605902711622-cfb43c44367f" class="d-block w-100 hero-img">
</div>

</div>

</div>

<!-- Hero Content Overlay -->
<div class="hero-content text-center text-white">
<h1 id="welcome"></h1>
<p>Play. Compete. Conquer.</p>

<button class="btn btn-warning m-2" data-bs-toggle="modal" data-bs-target="#loginModal">Login</button>
<button id="themeBtn" class="btn btn-light m-2">Toggle Theme</button>

<h5 id="clock" class="mt-3"></h5>

<button id="playBtn" class="btn btn-success mt-3">Play</button>
<p>Clicks: <span id="count">0</span></p>
</div>

</section>

<!-- LOGIN MODAL -->
<div class="modal fade" id="loginModal">
<div class="modal-dialog">
<div class="modal-content p-3">
<input type="text" id="username" class="form-control mb-2" placeholder="Enter Name">
<button onclick="login()" class="btn btn-primary">Login</button>
</div>
</div>
</div>

<!-- ================= GAMES ================= -->
<section id="games" class="container mt-5">
<h2 class="text-center">Game List</h2>

<input type="text" id="search" class="form-control my-3" placeholder="Search Game">
<button onclick="handleSearch()" class="btn btn-primary">Search</button>
<button onclick="sortGames()" class="btn btn-secondary">Sort by Rating</button>

<div id="gameList" class="d-flex flex-wrap justify-content-center"></div>
</section>

<!-- GAME MODAL -->
<div class="modal fade" id="gameModal">
<div class="modal-dialog">
<div class="modal-content p-3">
<h5 id="modalBody"></h5>
</div>
</div>
</div>

<!-- ================= PROFILE ================= -->
<section id="profile" class="mt-5">

<div class="profile-header text-center text-white">
<img src="https://images.unsplash.com/photo-1607746882042-944635dfe10e"
class="rounded-circle mt-4" width="120" height="120">
<h3 id="name">Alex</h3>
</div>

<div class="container text-center mt-4">
<h4>Score: <span id="score">1200</span></h4>
<button onclick="increaseScore()" class="btn btn-success m-2">+100</button>
<button onclick="resetProfile()" class="btn btn-danger m-2">Reset</button>
<button onclick="logout()" class="btn btn-warning m-2">Logout</button>

<div class="progress mt-3">
<div id="progressBar" class="progress-bar" style="width:50%"></div>
</div>

<button id="editBtn" class="btn btn-info mt-3">Edit Name</button>
</div>
</section>

<script>
/* HOME */
let playerName = "Guest";
document.getElementById("welcome").innerText = `Welcome ${playerName}`;

function login(){
let name = document.getElementById("username").value;
if(name.trim()===""){ alert("Name required!"); return; }
playerName = name;
document.getElementById("welcome").innerText = `Welcome ${playerName}`;
}

let darkMode=false;
document.getElementById("themeBtn").onclick=function(){
darkMode=!darkMode;
document.body.classList.toggle("bg-dark");
document.body.classList.toggle("text-white");
};

setInterval(()=>{
document.getElementById("clock").innerText = new Date().toLocaleTimeString();
},1000);

let count=0;
document.getElementById("playBtn").onclick=()=>{
count++;
document.getElementById("count").innerText=count;
};

/* GAMES */
class Game{
constructor(name,genre,rating,image){
this.name=name;
this.genre=genre;
this.rating=rating;
this.image=image;
}
}

const games=[
new Game("BattleZone","Action",4.5,"https://tse4.mm.bing.net/th/id/OIP.LjSBXxdpjp6PqadcjaARuwHaCc?rs=1&pid=ImgDetMain&o=7&rm=3"),
new Game("SpeedX","Racing",4.2,"https://i.ytimg.com/vi/BIBLjuyUjDs/mqdefault.jpg"),
new Game("MindPro","Puzzle",4.8,"https://th.bing.com/th/id/OIP.zriINMlRrOxLbNH9iXbWmAHaGB?w=217&h=180&c=7&r=0&o=7&dpr=1.3&pid=1.7&rm=3" )
];

function renderGames(list){
let container=document.getElementById("gameList");
container.innerHTML="";
list.forEach(game=>{
container.innerHTML+=`
<div class="card m-3" style="width:18rem;">
<img src="${game.image}" class="card-img-top" height="180">
<div class="card-body">
<h5>${game.name}</h5>
<p>${game.genre}</p>
<p>⭐ ${game.rating}</p>
<button class="btn btn-sm btn-primary like">Like</button>
<button class="btn btn-sm btn-warning" onclick="showDetails('${game.name}')">Details</button>
</div>
</div>`;
});
}
renderGames(games);

function handleSearch(){
let value=document.getElementById("search").value;
if(value===""){ alert("Search field empty"); return; }
let filtered=games.filter(g=>g.name.toLowerCase().includes(value.toLowerCase()));
renderGames(filtered);
}

function sortGames(){
games.sort((a,b)=>b.rating-a.rating);
renderGames(games);
}

let likes=0;
document.addEventListener("click",function(e){
if(e.target.classList.contains("like")){
likes++;
alert("Total Likes: "+likes);
}
});

function showDetails(name){
document.getElementById("modalBody").innerText=name;
new bootstrap.Modal(document.getElementById("gameModal")).show();
}

/* PROFILE */
const player={ name:"Alex", score:1200, updateScore(points){ this.score+=points; } };

function increaseScore(){
player.updateScore(100);
document.getElementById("score").innerText=player.score;
updateProgress();
}

function updateProgress(){
let percent=player.score/2000*100;
document.getElementById("progressBar").style.width=percent+"%";
}

$("#editBtn").click(()=>$("#name").prop("contentEditable",true));

function logout(){
if(confirm("Are you sure you want to logout?")){
location.reload();
}
}

function resetProfile(){
player.score=0;
document.getElementById("score").innerText=0;
updateProgress();
}
</script>

</body>
</html>
